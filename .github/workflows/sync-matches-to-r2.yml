# 已停用：数据同步已迁移至 Cloudflare Workers
name: Sync Matches to Cloudflare R2 (DISABLED)
on:
  workflow_dispatch:
  schedule:
    - cron: "*/3 * * * *"
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Action Disabled
        run: echo "This workflow has been disabled. Data is now synced by Cloudflare Workers."
